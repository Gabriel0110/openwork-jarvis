# Copy to `.env` for local development overrides if needed.
# Keep real secrets out of git.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# Optional local model endpoint (if used)
OLLAMA_BASE_URL=http://127.0.0.1:11434
